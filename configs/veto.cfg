"cfg_veto"
{
	/* Path of the veto map pool config file. */
	"map_pool_file" "addons/sourcemod/configs/veto_maplist.ini"

	/* Used for config compatibility. Please don't edit this value manually. */
	"version" 1
}
